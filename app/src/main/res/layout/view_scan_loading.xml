<?xml version="1.0" encoding="utf-8"?>
<!--
    Overlay de chargement affiché par-dessus l'écran d'accueil pendant le scan.
    Fond noir semi-transparent, spinner + texte centrés en blanc.
    clickable + focusable = bloque les clics qui passeraient à travers
    (l'utilisateur ne peut pas re-cliquer sur le bouton scan).
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#99000000"
    android:clickable="true"
    android:focusable="true"
    android:elevation="20dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center">

        <ProgressBar
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:indeterminateTint="@android:color/white" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="@string/scan_loading"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:letterSpacing="0.02" />

    </LinearLayout>

</FrameLayout>